{
  "name": "@junobuild/juno-action-e2e",
  "private": true,
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier --check .",
    "emulator:config": "node scripts/emulator.ts",
    "e2e": "NODE_ENV=development playwright test",
    "e2e:ci": "playwright test --reporter=html",
    "e2e:report": "playwright show-report",
    "e2e:playwright:install": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@junobuild/config": "^3.0.0",
    "@junobuild/emulator-playwright": "^0.1.1",
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.6.0",
    "prettier": "^3.8.2",
    "prettier-plugin-organize-imports": "^4.3.0"
  }
}
